<?php

/*
|--------------------------------------------------------------------------
| Application routes
|--------------------------------------------------------------------------
|
| Routes are owned by the modules: every module registers its own route files
| through its service provider (see app/Modules/{Module}/Routes). Keep this
| file empty so no route lives outside a module.
|
*/
